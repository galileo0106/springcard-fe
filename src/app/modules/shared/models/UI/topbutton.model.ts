/**
 * Declare a button for the top right of the screen
 */
export interface ITopButton {
  label: string;
  color?: string;
  icon?: string;
  type: string;
  id: string;
  tooltip?: string;
  disabled?: boolean;
}
