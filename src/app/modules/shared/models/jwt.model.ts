export interface IJwt {
  email: string;
  expire: string;
  token: string;
  isSocialAccount: boolean;
  socialProvider: string;
  Result: string;
  type: number;
  isAdmin: boolean;
  expiration_date: string;
}
